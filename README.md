# now-playing-bio

Puts the song you're currently listening to (from lastfm) in your bsky bio.

# Server

`pip install -r requirements.txt`

`mv .env.example .env`

Then fill out .env with the appropriate data.

yes polling is bad and this is janky but i havent been rate limited yet

probably should remove all the dependencies at some point (not really using them)
